/** @type {import('next').NextConfig} */
const nextConfig = {
  // Configuration simplifiée pour Next.js 15
  // Le module crypto natif de Node.js fonctionne maintenant côté client
};

export default nextConfig;